# go-job-queue

A simple Go program for a concurrent job queue where users can:

- create jobs
- enqueue them
- have worker goroutines process them
- store job results in memory
- query job status
- retry failed job
